import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  // HABILITAR EN CASO DE NECESITAR USAR SERVER ACTIONS -> AGUANTE HUAJA
  
  // experimental: {
  //   serverActions: true,
  // }
};

export default nextConfig;
